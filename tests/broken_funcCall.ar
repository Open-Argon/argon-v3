let x = do
    let class = {}
    class.f(path) = do
        let wowww = {}
        wowww.nice(callback) = callback()
        return wowww

    class.cool(path, callback) = do
        let c = (class.f(path))
        c.nice(callback)
    return class.object()


forever do
    x.cool("to", ()="epic")