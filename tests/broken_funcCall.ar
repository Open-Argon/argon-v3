let x = do
    let class = {}
    class.f(path) = do
        let wowww = {}
        wowww.nice(callback) = term.log(callback)
        return wowww

    class.cool(path, callback) = do
        term.log(path,callback)
        let c = (class.f(path))
        c.nice(callback)
    return class.object()
x.cool("to", ()=10)